name: Build and Release

on:
  push:
    branches: [main]
    tags:
      - 'v*'
      - '[0-9]+.[0-9]+.[0-9]+'
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: macos-latest
            platform: darwin
            arch: x64
          - os: macos-latest
            platform: darwin
            arch: arm64
          - os: ubuntu-latest
            platform: linux
            arch: x64
          - os: windows-latest
            platform: win32
            arch: x64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      # Cache Electron binary (fetched by electron's install.js, not npm)
      - name: Cache Electron binary (macOS / Linux)
        if: runner.os != 'Windows'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/electron
            ~/Library/Caches/electron
          key: electron-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: electron-${{ runner.os }}-

      - name: Cache Electron binary (Windows)
        if: runner.os == 'Windows'
        uses: actions/cache@v4
        with:
          path: ${{ env.LOCALAPPDATA }}\electron\Cache
          key: electron-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: electron-${{ runner.os }}-

      # Cache electron-builder tools (NSIS, winCodeSign, etc.)
      - name: Cache electron-builder tools (macOS / Linux)
        if: runner.os != 'Windows'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/electron-builder
            ~/Library/Caches/electron-builder
          key: electron-builder-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: electron-builder-${{ runner.os }}-

      - name: Cache electron-builder tools (Windows)
        if: runner.os == 'Windows'
        uses: actions/cache@v4
        with:
          path: ${{ env.LOCALAPPDATA }}\electron-builder\Cache
          key: electron-builder-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
          restore-keys: electron-builder-${{ runner.os }}-

      # Linux dependencies for Electron
      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y rpm dpkg fakeroot libx11-dev libxkbfile-dev

      # Install dependencies with retry — Electron binary download uses got directly,
      # not npm's HTTP client, so npm retry config vars have no effect on it
      - name: Install dependencies
        shell: bash
        run: |
          for attempt in 1 2 3; do
            npm ci && break
            echo "Attempt $attempt failed — retrying in 20s..."
            sleep 20
          done
        env:
          ELECTRON_GET_NO_PROGRESS: true

      # Build the app with electron-vite
      - name: Build app
        run: npm run build

      # Package for each platform — all wrapped in retry loops since electron-builder
      # downloads its own tools (NSIS, winCodeSign) from GitHub releases with no retry
      - name: Package (Linux)
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          for attempt in 1 2 3; do
            npx electron-builder --linux --x64 --publish never && break
            echo "Attempt $attempt failed — retrying in 20s..."
            sleep 20
          done

      - name: Package (Windows)
        if: matrix.platform == 'win32'
        shell: bash
        run: |
          for attempt in 1 2 3; do
            npx electron-builder --win --x64 --publish never && break
            echo "Attempt $attempt failed — retrying in 20s..."
            sleep 20
          done

      - name: Package (macOS x64)
        if: matrix.platform == 'darwin' && matrix.arch == 'x64'
        shell: bash
        run: |
          for attempt in 1 2 3; do
            npx electron-builder --mac --x64 --publish never && break
            echo "Attempt $attempt failed — retrying in 20s..."
            sleep 20
          done
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Package (macOS arm64)
        if: matrix.platform == 'darwin' && matrix.arch == 'arm64'
        shell: bash
        run: |
          for attempt in 1 2 3; do
            npx electron-builder --mac --arm64 --publish never && break
            echo "Attempt $attempt failed — retrying in 20s..."
            sleep 20
          done
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      # Upload artifacts
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: runlet-${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            dist/*.exe
            dist/*.dmg
            dist/*.AppImage
            dist/*.deb
            dist/*.rpm
            dist/*.zip
          if-no-files-found: ignore

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')

    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -R artifacts

      - name: Flatten artifacts
        run: |
          mkdir -p release
          find artifacts -type f \( -name "*.exe" -o -name "*.dmg" -o -name "*.zip" -o -name "*.AppImage" -o -name "*.deb" -o -name "*.rpm" \) -exec cp {} release/ \;
          ls -la release/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: release/*
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
